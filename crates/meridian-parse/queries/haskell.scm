; Definitions — a function equation's bound name (first variable child).
(function . (variable) @name) @def.function

; Calls — the head (first child) of a function application.
(apply . (variable) @call)

; Comments
(comment) @comment
