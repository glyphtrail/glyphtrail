[package]
name = "meridian-mcp"
authors.workspace = true
rust-version.workspace = true
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
meridian-core.workspace = true
meridian-store.workspace = true
anyhow.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true

[features]
# Serve queries against a LadybugDB index when one exists (#165). Pulls the
# lbug C++ build via meridian-store; off by default to keep the crate light.
ladybug = ["meridian-store/ladybug"]

[dev-dependencies]
assert2.workspace = true
